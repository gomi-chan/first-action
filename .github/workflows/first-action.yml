name: First Workflow
on:
  workflow_dispatch:
jobs:
  first-job:
    runs-on: macos-latest
    steps:
      - name: Print greeting
        run: echo "hello"
      - name: Print goodbye
        run: echo "goodbye"
